import { NgModule } from '@angular/core';
import { CommonModule } from '@angular/common';
import { MatRouting } from './mat.routing';

@NgModule({
    imports: [CommonModule, MatRouting]
})
export class MatModule {}
